apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.config.payment.name }}
  labels:
    app.kubernetes.io/name: "{{ .Values.config.payment.name }}"  
    helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    app.kubernetes.io/managed-by: "{{ .Release.Service  }}"
    app.kubernetes.io/instance: "{{ .Release.Name }}"   
spec:
  type: {{ .Values.payment.service.type }}
  selector:
    app: {{ .Values.config.payment.name }}
  ports:
  - name: {{ .Values.payment.service.name }}
    port: {{ .Values.config.payment.externalPort }}
    targetPort: {{ .Values.config.payment.internalPort }}
    
  
