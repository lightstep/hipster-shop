apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.config.shipping.name }}
  labels:
    app.kubernetes.io/name: "{{ .Values.config.shipping.name }}"  
    helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    app.kubernetes.io/managed-by: "{{ .Release.Service  }}"
    app.kubernetes.io/instance: "{{ .Release.Name }}"   
spec:
  type: {{ .Values.shipping.service.type }}
  ports:
  - port: {{ .Values.config.shipping.externalPort }}
    targetPort: {{ .Values.config.shipping.internalPort }}
    protocol: TCP
    name: {{ .Values.shipping.service.name }}
  selector:
    app: {{ .Values.config.shipping.name }}
